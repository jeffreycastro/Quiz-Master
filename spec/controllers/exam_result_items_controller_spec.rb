require 'rails_helper'

# This spec was generated by rspec-rails when you ran the scaffold generator.
# It demonstrates how one might use RSpec to specify the controller code that
# was generated by Rails when you ran the scaffold generator.
#
# It assumes that the implementation code is generated by the rails scaffold
# generator.  If you are using any extension libraries to generate different
# controller code, this generated spec may or may not pass.
#
# It only uses APIs available in rails and/or rspec-rails.  There are a number
# of tools you can use to make these specs even more expressive, but we're
# sticking to rails and rspec-rails APIs to keep things simple and stable.
#
# Compared to earlier versions of this generator, there is very limited use of
# stubs and message expectations in this spec.  Stubs are only used when there
# is no simpler way to get a handle on the object needed for the example.
# Message expectations are only used when there is no simpler way to specify
# that an instance is receiving a specific message.

RSpec.describe ExamResultItemsController, type: :controller do

  before(:each) do
    @exam = Exam.create!(name: "Exam name")
    @exam_result = @exam.exam_results.create
    @exam_item = @exam.exam_items.create!(question: "Q1", seq_num: 1)
  end

  # This should return the minimal set of attributes required to create a valid
  # ExamResultItem. As you add validations to ExamResultItem, be sure to
  # adjust the attributes here as well.
  let(:valid_attributes) {
    {
      exam_result_id: @exam_result.id,
      exam_item_id: @exam_item.id,
      answer: ""
    }
  }

  let(:invalid_attributes) {
    {
      exam_result_id: nil,
      exam_item_id: nil,
      answer: ""
    }
  }

  # This should return the minimal set of values that should be in the session
  # in order to pass any filters (e.g. authentication) defined in
  # ExamResultItemsController. Be sure to keep this updated too.
  let(:valid_session) { {} }

  describe "GET #index" do
    it "assigns all exam_result_items as @exam_result_items" do
      exam_result_item = ExamResultItem.create! valid_attributes
      get :index, params: {}, session: valid_session
      expect(assigns(:exam_result_items)).to eq([exam_result_item])
    end
  end

  describe "GET #show" do
    it "assigns the requested exam_result_item as @exam_result_item" do
      exam_result_item = ExamResultItem.create! valid_attributes
      get :show, params: {id: exam_result_item.to_param}, session: valid_session
      expect(assigns(:exam_result_item)).to eq(exam_result_item)
    end
  end

  describe "GET #new" do
    it "assigns a new exam_result_item as @exam_result_item" do
      get :new, params: {}, session: valid_session
      expect(assigns(:exam_result_item)).to be_a_new(ExamResultItem)
    end
  end

  describe "GET #edit" do
    it "assigns the requested exam_result_item as @exam_result_item" do
      exam_result_item = ExamResultItem.create! valid_attributes
      get :edit, params: {id: exam_result_item.to_param}, session: valid_session
      expect(assigns(:exam_result_item)).to eq(exam_result_item)
    end
  end

  describe "POST #create" do
    context "with valid params" do
      it "creates a new ExamResultItem" do
        expect {
          post :create, params: {exam_result_item: valid_attributes}, session: valid_session
        }.to change(ExamResultItem, :count).by(1)
      end

      it "assigns a newly created exam_result_item as @exam_result_item" do
        post :create, params: {exam_result_item: valid_attributes}, session: valid_session
        expect(assigns(:exam_result_item)).to be_a(ExamResultItem)
        expect(assigns(:exam_result_item)).to be_persisted
      end

      it "redirects to the created exam_result_item" do
        post :create, params: {exam_result_item: valid_attributes}, session: valid_session
        expect(response).to redirect_to(ExamResultItem.last)
      end
    end

    context "with invalid params" do
      it "assigns a newly created but unsaved exam_result_item as @exam_result_item" do
        post :create, params: {exam_result_item: invalid_attributes}, session: valid_session
        expect(assigns(:exam_result_item)).to be_a_new(ExamResultItem)
      end

      it "re-renders the 'new' template" do
        post :create, params: {exam_result_item: invalid_attributes}, session: valid_session
        expect(response).to render_template("new")
      end
    end
  end

  describe "PUT #update" do
    context "with valid params" do
      let(:new_attributes) {
        skip("Add a hash of attributes valid for your model")
      }

      it "updates the requested exam_result_item" do
        exam_result_item = ExamResultItem.create! valid_attributes
        put :update, params: {id: exam_result_item.to_param, exam_result_item: new_attributes}, session: valid_session
        exam_result_item.reload
        skip("Add assertions for updated state")
      end

      it "assigns the requested exam_result_item as @exam_result_item" do
        exam_result_item = ExamResultItem.create! valid_attributes
        put :update, params: {id: exam_result_item.to_param, exam_result_item: valid_attributes}, session: valid_session
        expect(assigns(:exam_result_item)).to eq(exam_result_item)
      end

      it "redirects to the exam_result_item" do
        exam_result_item = ExamResultItem.create! valid_attributes
        put :update, params: {id: exam_result_item.to_param, exam_result_item: valid_attributes}, session: valid_session
        expect(response).to redirect_to(exam_result_item)
      end
    end

    context "with invalid params" do
      it "assigns the exam_result_item as @exam_result_item" do
        exam_result_item = ExamResultItem.create! valid_attributes
        put :update, params: {id: exam_result_item.to_param, exam_result_item: invalid_attributes}, session: valid_session
        expect(assigns(:exam_result_item)).to eq(exam_result_item)
      end

      it "re-renders the 'edit' template" do
        exam_result_item = ExamResultItem.create! valid_attributes
        put :update, params: {id: exam_result_item.to_param, exam_result_item: invalid_attributes}, session: valid_session
        expect(response).to render_template("edit")
      end
    end
  end

  describe "DELETE #destroy" do
    it "destroys the requested exam_result_item" do
      exam_result_item = ExamResultItem.create! valid_attributes
      expect {
        delete :destroy, params: {id: exam_result_item.to_param}, session: valid_session
      }.to change(ExamResultItem, :count).by(-1)
    end

    it "redirects to the exam_result_items list" do
      exam_result_item = ExamResultItem.create! valid_attributes
      delete :destroy, params: {id: exam_result_item.to_param}, session: valid_session
      expect(response).to redirect_to(exam_result_items_url)
    end
  end

end
