%p#notice= notice

%p
  %b Name:
  = @exam.name

  %table
    %thead
      %tr
        %th #
        %th Question
        %th Answers

    %tbody
      - @exam.exam_items.each_with_index do |ei, index|
        %tr
          %td= index += 1
          %td= ei.question
          %td= possible_answers_display(ei).html_safe

= link_to 'Edit', edit_exam_path(@exam)
\|
= link_to 'Back', exams_path
