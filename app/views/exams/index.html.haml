%h1 Listing exams

%table
  %thead
    %tr
      %th Name
      %th
      %th
      %th
      %th

  %tbody
    - @exams.each do |exam|
      %tr
        %td= exam.name
        %td= link_to 'Show', exam
        %td= link_to 'Edit', edit_exam_path(exam)
        %td= link_to 'Destroy', exam, :method => :delete, :data => { :confirm => 'Are you sure?' }
        %td= link_to 'Take Exam', take_exam_exam_results_path(exam_id: exam.id)

%br

= link_to 'New Exam', new_exam_path
